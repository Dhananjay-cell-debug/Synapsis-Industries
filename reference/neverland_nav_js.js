// This is the Neverland navigation/footer JS code provided by the user.
// It contains:
// 1. Multiple SVG icons for social media (LinkedIn, Dribbble, Instagram, Behance).
// 2. A Footer component (A1) containing social links and terms links.
// 3. A Scroll Navigation Button (P1) that jumps to sections.
// 4. A Header/Menu layout (y3) that uses GSAP ScrollTrigger to track the active section (`activeScroll` index) as the user scrolls.

// Key takeaway: We can implement a vertical right-side scroll navigation bar
// that highlights the current section (01, 02, 03) based on scroll depth.
